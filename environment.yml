name: align
channels:
  - conda-forge
  - defaults

dependencies:
  - python=3.10
  - pip
  - setuptools
  - wheel

  - pip:
      - openai>=1.30.0
      - langchain>=1.2
      - langchain-core>=1.3
      - langchain-openai>=1.1
      - langgraph>=1.1
      - composio-core>=0.7
      - composio-langchain>=0.11
      - python-dotenv==1.0.0
      - streamlit==1.28.0
      - pandas==2.0.3
      - pytest
