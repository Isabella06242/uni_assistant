name: align
channels:
  - conda-forge
  - defaults

dependencies:
  - python=3.10
  - pip
  - setuptools
  - wheel

  - pip:
      - openai>=1.30.0
      - langchain==0.1.20
      - langchain-core==0.1.52
      - langchain-openai==0.1.7
      - langchain-community==0.0.38
      - python-dotenv==1.0.0
      - streamlit==1.28.0
      - pandas==2.0.3
      - pytest
